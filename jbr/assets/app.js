const JBR=(()=>{const KEYS={ideas:'jbr_ideas_v1',applications:'jbr_applications_v1'};const state={challenges:[]};const $=(s,r=document)=>r.querySelector(s),$$=(s,r=document)=>[...r.querySelectorAll(s)];function safeLoad(k){try{return JSON.parse(localStorage.getItem(k)||'[]')}catch{return[]}}function safeSave(k,v){localStorage.setItem(k,JSON.stringify(v))}async function loadChallenges(){try{const r=await fetch('data/challenges.json',{cache:'no-store'});if(!r.ok)throw new Error('Challenge data unavailable');state.challenges=await r.json();renderChallenges()}catch(e){$('#challenge-grid').innerHTML='<div class="empty-state">Discovery challenge data could not be loaded. Please refresh the page.</div>';console.error(e)}}function renderChallenges(){const grid=$('#challenge-grid'),q=($('#challenge-search')?.value||'').trim().toLowerCase(),status=$('#challenge-status')?.value||'all';const filtered=state.challenges.filter(c=>{const h=[c.title,c.summary,...(c.disciplines||[]),...(c.expertise_needed||[])].join(' ').toLowerCase();return(!q||h.includes(q))&&(status==='all'||c.status===status)});if(!filtered.length){grid.innerHTML='<div class="empty-state">No challenge matches these filters yet.</div>';return}grid.innerHTML=filtered.map(c=>`<article class="challenge-card"><div class="challenge-top"><span class="challenge-id">${esc(c.id)}</span><span class="status">${esc(c.status)}</span></div><h3>${esc(c.title)}</h3><p>${esc(c.summary)}</p><div class="tag-row">${(c.disciplines||[]).slice(0,5).map(x=>`<span class="tag">${esc(x)}</span>`).join('')}</div><p><strong>Expertise sought:</strong> ${esc((c.expertise_needed||[]).join(', '))}</p><div class="challenge-actions"><button class="btn btn-primary apply-btn" data-challenge="${esc(c.id)}">Express interest</button><button class="btn btn-secondary details-btn" data-challenge="${esc(c.id)}">View scope</button></div></article>`).join('');$$('.apply-btn').forEach(b=>b.addEventListener('click',()=>openApplication(b.dataset.challenge)));$$('.details-btn').forEach(b=>b.addEventListener('click',()=>showDetails(b.dataset.challenge)))}function showDetails(id){const c=state.challenges.find(x=>x.id===id);if(!c)return;alert(`${c.title}\n\nResearch gap:\n${c.research_gap}\n\nWorking hypothesis:\n${c.hypothesis}\n\nExpected outputs:\n${c.expected_outputs.join('; ')}`)}function openApplication(id){const c=state.challenges.find(x=>x.id===id);if(!c)return;const d=$('#apply-dialog');$('#application-challenge').textContent=`${c.id} — ${c.title}`;$('#application-form [name=challengeId]').value=c.id;d.showModal()}function bindApplicationForm(){const f=$('#application-form');f.addEventListener('submit',e=>{e.preventDefault();const rec=Object.fromEntries(new FormData(f).entries());rec.id=`EOI-${Date.now()}`;rec.created_at=new Date().toISOString();const records=safeLoad(KEYS.applications);records.push(rec);safeSave(KEYS.applications,records);f.reset();$('#apply-dialog').close();alert('Expression of interest saved in this browser. Shared submission will activate when the JBR database is connected.')})}function bindIdeaForm(){const f=$('#idea-form'),status=$('#form-status');f.addEventListener('submit',e=>{e.preventDefault();const rec=Object.fromEntries(new FormData(f).entries());rec.id=`IDEA-${Date.now()}`;rec.created_at=new Date().toISOString();rec.stage='Draft submitted from MVP';const records=safeLoad(KEYS.ideas);records.push(rec);safeSave(KEYS.ideas,records);f.reset();status.textContent=`Saved ${rec.id}. You can export your drafts for review.`});$('#export-ideas').addEventListener('click',()=>{const records=safeLoad(KEYS.ideas);if(!records.length){status.textContent='No saved research ideas in this browser yet.';return}const blob=new Blob([JSON.stringify(records,null,2)],{type:'application/json'}),url=URL.createObjectURL(blob),a=document.createElement('a');a.href=url;a.download='jbr-research-ideas.json';a.click();URL.revokeObjectURL(url);status.textContent=`Exported ${records.length} saved research idea(s).`})}function bindNavigation(){const t=$('.nav-toggle'),n=$('#primary-nav');t.addEventListener('click',()=>{const o=n.classList.toggle('open');t.setAttribute('aria-expanded',String(o))});$$('#primary-nav a').forEach(a=>a.addEventListener('click',()=>{n.classList.remove('open');t.setAttribute('aria-expanded','false')}))}function esc(v=''){return String(v).replace(/[&<>"']/g,ch=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#039;'}[ch]))}function init(){$('#year').textContent=new Date().getFullYear();bindNavigation();bindIdeaForm();bindApplicationForm();$('#challenge-search').addEventListener('input',renderChallenges);$('#challenge-status').addEventListener('change',renderChallenges);loadChallenges()}return{init}})();document.addEventListener('DOMContentLoaded',JBR.init);
