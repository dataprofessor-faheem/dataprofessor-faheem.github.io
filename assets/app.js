const qs=s=>document.querySelector(s);async function loadContent(){try{const r=await fetch(`data/content.json?v=${Date.now()}`);const data=await r.json();qs('#heroTitle').textContent=data.site.title;qs('#heroSubtitle').textContent=data.site.subtitle;qs('#aboutText').textContent=data.site.about;qs('#contactText').textContent=data.site.contact_text;qs('#contactBtn').href=`mailto:${data.site.email}`;qs('#projectCount').textContent=data.projects.length;qs('#publicationCount').textContent=data.publications.length;qs('#datasetCount').textContent=data.datasets.length;qs('#projectsGrid').innerHTML=data.projects.map(p=>`<article class="card"><span class="tag">${escapeHtml(p.category)}</span><h3>${escapeHtml(p.title)}</h3><p>${escapeHtml(p.description)}</p>${p.url?`<a class="btn" href="${p.url}" target="_blank" rel="noopener">Open project</a>`:''}</article>`).join('');qs('#publicationList').innerHTML=data.publications.map(p=>`<div class="list-item"><strong>${escapeHtml(p.title)}</strong><span class="muted">${escapeHtml(p.citation)}</span></div>`).join('');qs('#datasetTable').innerHTML=data.datasets.map(d=>`<tr><td><strong>${escapeHtml(d.name)}</strong></td><td>${escapeHtml(d.description)}</td><td>${escapeHtml(d.updated)}</td><td>${d.url?`<a href="${d.url}" target="_blank" rel="noopener">Open</a>`:'—'}</td></tr>`).join('')}catch(e){console.error(e);qs('#aboutText').textContent='Content could not be loaded. Check data/content.json.'}}function escapeHtml(value=''){return String(value).replace(/[&<>"']/g,ch=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#039;'}[ch]))}qs('#menuBtn').addEventListener('click',()=>qs('#navLinks').classList.toggle('open'));document.querySelectorAll('#navLinks a').forEach(a=>a.addEventListener('click',()=>qs('#navLinks').classList.remove('open')));qs('#year').textContent=new Date().getFullYear();loadContent();